# Ray and vLLM are explicit resolver roots in Dockerfile. Native CUDA packages
# are constrained to the immutable base's versions; Python dependencies still
# participate in normal resolution and the final environment must pass pip check.
httpx==0.28.1
# The cu129 base installs NCCL 2.30.7, but its torch 2.13.0+cu129 wheel
# requires exactly 2.29.7. Repair that measured base inconsistency normally.
nvidia-nccl-cu12==2.29.7
# Intersection of vLLM 0.29 common requirements and Ray 2.58 Serve requirements.
# Do not restore the old image's 1.26 pins or downgrade patched base packages.
opentelemetry-api>=1.30.0,<2
opentelemetry-sdk>=1.30.0,<2
opentelemetry-exporter-otlp>=1.30.0,<2
fastapi>=0.133.0,<0.137.0
# Patched minimums from the candidate audit, above upstream's functional floor.
starlette>=1.3.1,<2
prometheus-fastapi-instrumentator>=8.0.0,<9
aiohttp>=3.14.3,<4
