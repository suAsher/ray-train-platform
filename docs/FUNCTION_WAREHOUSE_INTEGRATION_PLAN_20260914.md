# 训练平台自动同步功能仓：实施计划与接口梳理

2026-09-14：连接器、后台同步及 schema 53 已发布。开发仓手动同步已完成浏览器创建、三个来源 ID 读回及文件 SHA-256 验收。用户入口随后收敛为固定正式功能仓，最新发布和自动同步证据见文末。第 2～14 节保留接口调研及开发过程，早期“建议/待验证”描述不是最新交付状态。

## 1. 用户最终如何使用

目标：用户只需选择权重与目标功能仓；平台负责复制校验、上传、登记版本，并让两边都能追溯训练任务、MLflow Run、实验和数据来源。

本次完整验收目标是新训练任务自动同步。一键同步用于历史任务和联调，两者共用一套后台处理流程；只交付一键同步不能算完成本需求。自动模式由平台内部生成模型副本与版本，不要求用户训练结束后再手动登记、审批或点击同步。

**已有训练结果：**

1. 用户在「本人任务详情 → 训练产物」选择权重，点击「同步到功能仓」。
2. 选择自己有写权限的正式功能仓和模型类型，确认文件与版本名，点击「开始同步」。不需要选择环境、手填三个 ID、先登记共享模型或申请发布。
3. 平台自动生成不可变副本、复制并核验 SHA-256；副本 `READY` 后后台上传和创建功能仓版本。
4. 任务详情显示同步记录、三个来源 ID 和目标版本 ID。创建后读回一致才显示成功，随后可打开正式功能仓。用户允许主动多次同步不同文件，每次创建新版本；同一次请求重试使用幂等键。

**新训练任务：**

1. 提交任务后，在运行中任务的「训练产物 → 同步到功能仓」配置目标和输出文件路径，点击「保存自动同步」。当前入口在任务详情，提交表单没有第二套配置。
2. 用户指定明确的输出相对路径，例如 `checkpoints/best.pth`。不默认递归同步整个输出目录，不根据文件名推断哪份权重最好。
3. 任务成功结束后，平台核对文件、生成不可变模型版本，待 `READY` 后自动启动同一同步流程。
4. 用户在任务详情查看同步进度；授权失效时重新登录后重试，不需要重新训练。仅有 OAuth access token，没有独立刷新凭据，因此长训练不能承诺全程免重新授权。

训练节点只写现有平台输出目录。向功能仓的传输由受控的后台 CPU 任务执行，不要求训练节点访问公网，也不向训练 Pod 注入功能仓凭据。

## 2. 功能仓与发布仓的关系

本轮补查了独立 Portal 的 `src/api/model/modalStore/publish.js`、`pubStore.vue` 和 `pubVersion.vue`。以下是页面及前端调用合同确认的关系，不代表发布仓后端已经生产写入验收。

| 系统 | 职责与当前可确认行为 | 本次范围 |
| --- | --- | --- |
| 训练平台 | 执行训练，记录 Job 与 MLflow Run，保存权重及来源 | 训练成功后自动同步指定产物 |
| 功能仓 | 保存模型类型和模型版本、文件；提供评估与模型转换入口 | 接收实际文件，创建版本，保存/查询/展示训练来源 |
| 发布仓 | 从已有模型版本选文件，组合为面向设备类型的发布版本 | 本次不自动创建发布版本，不改发布规则 |

发布版本创建调用 `POST /model/issueVersion/create`，前端提交 `issueWarehouseId`、`groupId`、`version`、`deviceType` 和 `modelFiles: [{modelVersionId, modelFileIds}]` 等字段。选文件时按 `convertFiles.deviceType` 匹配，缺少对应文件时提示先到功能仓发起模型转换。因此自动上传训练权重不等于已产生可发布的转换文件。

最终关系为：训练任务 → 功能仓模型版本 → 后续用户按既有评估/转换/发布流程选择该版本 → 发布仓版本。训练来源固定在功能仓模型版本上，发布仓可通过所引用的 `modelVersionId` 追溯；是否需要新增发布仓来源展示属于后续独立需求。

## 3. 已核对的真实能力及缺口

2026-09-14 浏览器访问了正式 Portal 功能仓详情。该页面存在模型版本列表、上传、查看、编辑、评估与转换入口。浏览器网络记录确认功能仓详情、版本分页和模型类型查询曾返回 200；出现过会话到期的 401，因此不能把浏览器登录作为稳定的后台认证。

以下接口名称来自独立 Portal 源码；上传/创建路径存在于前端调用中，不代表已用服务身份完成生产写入验收。

| 用途 | 现有前端调用 | 当前可确认范围 |
| --- | --- | --- |
| 选择功能仓 | `GET /model/functionWarehouse/page` | `groupId`、关键词、可见性、分页 |
| 功能仓详情 | `GET /model/functionWarehouse` | `functionWarehouseId` |
| 模型类型 | `GET /model/model/type/list` | `functionWarehouseId`、`groupId` |
| 上传文件 | `POST /system/api/file/minio/upload` | multipart 文件和 prefix |
| 分片上传/合并 | `POST /system/api/file/minio/chunk/upload`、`POST /system/api/file/minio/chunk/merge` | 已有分片、MD5、SHA-256 和合并调用；需后端确认服务身份适用性与会话隔离 |
| 创建模型版本 | `POST /model/model/version/create` | `functionWarehouseId`、`groupId`、`modelTypeId`、`version`、`description`、`production`、`paths` |
| 版本查询 | `GET /model/model/version/page` | 可分页查看；按来源/幂等键精确查询仍需确认 |

`paths` 中前端实际提交的文件字段是 `url`、`filename`、`fileSha256`、`fileSize`。已有路径转存接口 `/system/api/file/upload2minio`，但没有证据证明其后端能访问训练集群的 FSX 路径；不把它当作可直接使用的跨集群复制能力。

目前前端创建版本合同未见 `job_id/run_id/experiment_id` 或通用训练来源对象。已有 `mlflowViewUrl` 出现在评估报告/发布展示链路，不能直接覆盖成训练 Run 地址，混淆两种实验来源。

训练平台现有 `modellifecycle.Version` 包含 Job ID、可选 Run ID、模型版本 ID、文件长度与 SHA-256、代码/镜像及数据来源；当前不含 Experiment ID。同步前需要从已关联 Run 查询真实 Experiment ID，并固定到同步来源快照。

## 4. ID 与来源合同

三个 ID 不要求相等，也不能相互推算：

| 字段 | 意义与来源 |
| --- | --- |
| `jobId` | 平台训练任务 ID |
| `runId` | 产生这份权重的 MLflow Run ID，必须使用已核实的关联 |
| `experimentId` | 从该 Run 的信息读取其所属实验 ID，按字符串传输 |
| `trackingInstanceId` | 标识来源 MLflow 实例，避免跨环境 ID 混淆 |
| `platformModelVersionId` | 平台不可变模型版本，标识文件来源；同一版本允许主动同步多次 |
| `targetModelVersionId` | 功能仓创建后返回的模型版本 ID |
| `fileSha256`、`fileSize` | 双方校验后相同的权重内容身份 |

建议功能仓增加结构化 `trainingSource`，字段名由双方确认。示意载荷如下，不能直接调用线上接口：

```json
{
  "functionWarehouseId": "<目标功能仓ID>",
  "groupId": "<目标团队ID>",
  "modelTypeId": "<目标模型类型ID>",
  "version": "<用户确认的版本名>",
  "production": false,
  "paths": [{
    "url": "<功能仓上传接口返回的文件定位符>",
    "filename": "best.pth",
    "fileSha256": "<完整SHA256>",
    "fileSize": 123456
  }],
  "trainingSource": {
    "sourceType": "raytrain",
    "platformInstanceId": "<平台实例ID>",
    "platformModelVersionId": "<平台模型版本ID>",
    "jobId": "<平台JobID>",
    "trackingInstanceId": "<MLflow实例ID>",
    "runId": "<MLflowRunID>",
    "experimentId": "<真实ExperimentID>"
  }
}
```

可选携带代码摘要、提交号、镜像摘要、数据集及数据版本、manifest 摘要，并区分系统记录与用户补充。未知来源保留未知；不擅自补写历史 Run。业务数据样本不随权重同步。

一个 Job 可有多个 Run，续训也可能使用新的 Job/Run。导出必须对应产生所选文件的 Run；关联缺失或有歧义时显示「待确认训练来源」，不能选最近 Run 凑齐 ID。首批自动同步不生成缺少必要来源的“成功关联”版本。旧任务可由用户确认经过验证的来源后手动同步。

## 5. 功能仓后端需要确认的五组能力

尽量复用现有接口，不要求对方按建议路径重写。最低要求是下面的行为与返回值明确。

| 能力 | 请求/权限 | 必需返回与保证 |
| --- | --- | --- |
| 服务认证与目标权限 | 已验证可转发用户 OAuth2 access token；限定可写团队、功能仓；后台任务另需解决长时有效的用户委托或服务授权 | 用户请求转发方式已只读验证；仍需明确过期后的续期、后台授权及写权限。不能保存个人 Cookie充当后台认证，也不能假定 RayTrain PAT 可登录功能仓 |
| 可写目标查询 | 按当前授权查询团队、功能仓、模型类型 | 稳定 ID、名称和写权限；服务账号能写不等于任意平台用户能写 |
| 文件传输及校验 | 小文件或分片；上传会话绑定身份和目标；传入长度、摘要、幂等键 | 会话/文件 ID、已上传分片、过期时间、最终长度与 SHA-256；明确最大文件/分片大小、限流和清理机制 |
| 创建版本 | 已验证文件引用、版本信息、结构化训练来源；建议支持操作 ID | 目标版本 ID、页面 URL、持久化来源；允许相同 Job/Run 多次创建。若支持操作 ID，同操作重试返回原版本，新操作创建新版本；创建与来源落库应原子完成 |
| 精确查询及对账 | 按目标版本 ID 查询；建议支持操作 ID 查询 | 文件校验结果、来源字段、处理状态、错误原因。若不能按操作查询，创建响应丢失时转待确认，不自动再次创建 |

上传完成与版本登记是两项结果，后端需说明：版本创建失败时已上传文件如何复用、如何按上传会话过期清理。不能用“重新 POST 创建”处理所有超时，否则会产生重复版本和孤立文件。

## 6. 后台流程与失败恢复

```text
用户选择目标/配置自动同步
→ 确认任务终态、文件规则、管理权限、目标写权限与 Run 关联
→ 复用平台模型副本复制和 SHA-256 校验
→ READY 版本 + 固定训练来源 + 目标配置快照
→ 持久化同步记录
→ 后台上传/断点续传 → 目标校验 → 幂等登记版本 → 精确查询对账
→ 展示成功、目标链接与三个来源 ID
```

建议内部状态：`WAITING_SOURCE → QUEUED → UPLOADING → VERIFYING → REGISTERING → SUCCEEDED`；失败保留具体阶段。仅网络/限流/明确可重试服务端错误进入有上限的退避重试。认证、权限、摘要不符、目标删除、来源冲突需人工处理，不能无上限循环。

- 用数据库持久化记录和租约，复用平台后台 worker 模式；不用只存在内存里的 goroutine 保证可靠性。
- 用户每次主动发起同步都分配新的操作 ID，允许同一 Job、Run 或平台模型版本再次创建功能仓版本，且每次可以选择不同文件。不能把 Job ID、Run ID 或文件名当作全局去重键。
- 同一次操作的网络重试、提交期间的双击和后台恢复沿用操作 ID；请求摘要覆盖本次文件清单、摘要、目标、版本名及来源。只有操作重试去重，用户明确发起的新操作不去重。
- 创建请求超时先按操作 ID 对账；若对方尚无幂等及精确查询能力，则标记「登记结果待确认」，停止自动重发创建请求。用户核对目标后可明确发起新的同步，平台不把响应丢失伪装成失败后盲目重建。
- 只传已校验的不可变平台副本，不反复从仍在变化的个人输出路径上传。
- 自动模式仅在成功结束后执行。失败/取消任务保留的 checkpoint 可走用户明确选择的手动流程，并标明来源任务状态；不能自动当作成功训练成果。
- 一个明确文件规则对应一个候选文件；零个显示未找到，多个要求选择，避免静默“取最新”。首批不默认上传全部 epoch。
- 后台传输设置并发和带宽上限、有限缓冲，复用现有 CPU 工作节点；不占 GPU、不阻塞训练结束或状态协调。
- `production=false` 作为自动导入的建议默认值，需双方确认其业务含义。同步不会自动评估、转换、审批、发布或启动推理。
- 平台归档不自动删除功能仓版本。取消同步不回滚已完成的外部版本；已上传未登记文件按目标上传会话清理合同处理，不能删除用户既有文件。

## 7. UI 与可见性

保持现有一级菜单，在模型版本详情加「功能仓同步」区域；任务详情展示对应模型版本与同步结果。区域包含目标仓/模型/版本、当前阶段、进度、更新时间、失败原因、「重试」「查看功能仓」及来源跳转。

模型和同步元数据沿用平台共享可见规则；发起/修改同步由模型管理权限控制。目标功能仓的公开、私有、被邀请者权限仍由功能仓决定，不因源模型共享而扩大目标可见性。页面可显示目标名称及“需要目标仓权限”，不暴露可绕过权限的下载 URL。

普通使用说明新增/补充问题文章：「训练权重如何同步功能仓」「三个来源 ID 是什么」「自动同步失败如何重试」「续训产生的版本如何关联」。管理员凭据配置留在运维文档，不塞进用户接入步骤。

## 8. 分三批实施

| 批次 | 交付 | 完成门槛 |
| --- | --- | --- |
| 一：接口合同与测试目标 | 确认上述五组能力、团队映射、测试功能仓、认证和来源字段 | 用户委托可读可写；来源可持久化读回；大文件协议和校验明确，登记响应丢失有明确处置 |
| 二：已有模型一键同步 | 后台任务、数据库记录、Portal 状态/按钮、用户帮助 | 专用模型完整上传和来源跳转；超时重试/权限拒绝/摘要错误/重启恢复可验证；同操作不意外重复，新操作可再次创建 |
| 三：训练完成自动同步 | Web/CLI 可选配置、文件规则、成功终态触发与持久恢复 | 一次配置后自动走通；失败任务不误传、缺来源不造假、重复事件只导入一次、其他训练不受影响 |

数据库迁移编号、组件构建范围、发布版本须在开始实现时重新核对，不能根据本计划固定。预计涉及后端及数据库、独立 Portal `dev`；若开放 CLI 配置则同时更新 `spk-rayjob` 下载组件。不改用户训练代码、不把代码放入训练镜像。

开发仍按候选先送构建机验证、通过后同步四端、只构建变更组件的流程。需要迁移时先完成该次获准的备份与隔离恢复验证；本轮计划不授权生产数据导出或业务权重验收。

## 9. 最低验收清单

1. 专用小权重和代表性大文件传输成功，源/目标字节数及 SHA-256 相同。
2. 功能仓读回完整 Job/Run/Experiment 及平台版本来源，页面链接打开对应对象；评估 Run 地址不被训练 Run 覆盖。
3. 同操作的重复提交、重复事件和进程重启不意外产生重复版本；用户主动创建新操作可再次同步同一 Job/Run，并可选择不同文件。登记响应丢失时先对账或提示待确认，不盲目重发；不同内容复用同一操作 ID 被拒绝。
4. 源模型无管理权限、目标无写权限、凭据失效、目标被删除均明确失败；不越权上传。
5. 没有 Run、Run 关联歧义、缺文件、多文件、仍在写入、摘要不符均不能显示同步成功。
6. 自动同步关闭时行为不变；旧 Web/CLI 请求无新增必填字段；历史数据不自动补传。
7. 开关关闭可停止接受新同步；已有记录可审计、可恢复，关闭功能不删除模型或功能仓版本。
8. 发布前后核对运行任务的 RayJob/RayCluster/Pod UID、状态和重启数；训练与 MLflow 继续工作。

## 10. 当前证据位置

- 功能仓 API：独立 Portal `src/api/model/modalStore/function.js`。
- 版本表单与上传返回：`src/views/model/modelStore/components/modelStore/actionModal.vue`，`src/components/ChunkUploader/index.vue`。
- 平台副本与来源：`backend/modellifecycle/types.go`、`service.go`，`backend/api/model_lifecycle_versions.go`，`backend/repositories/model_lifecycle.go`。
- 正式功能仓页面已只读查看；没有把当前浏览的他人业务仓作为默认写入目标。

已提供正式与开发 groupId、OAuth2 用户令牌转发方式及 dev `/model/v2/api-docs`。功能仓后端现已支持三个顶层来源字段，实际创建与分页读回均验证通过（详见第14节）。后台续期尚无接口合同；首批凭据过期必须转待重新授权。密钥不写入聊天、Git 或普通使用说明。

## 11. OAuth2 用户令牌只读联调结果

用户提供的机制：将可信 oauth2-proxy 转发的 `X-Auth-Request-Access-Token` 作为功能仓请求的 `Authorization: Bearer <token>`。

2026-09-14 已在已登录 Portal 浏览器中验证，调用 `/oauth2/auth` 获取同名响应头，令牌仅在函数内存中使用，没有输出令牌、写文件或持久保存。每次模型查询使用 `credentials: omit`，排除依赖浏览器 Cookie 的误判，并禁止自动跟随重定向。

| 验证 | 结果 |
| --- | --- |
| 正式 Portal `/oauth2/auth` | HTTP 202，返回 access-token 头 |
| 正式功能仓模型类型查询，无 Cookie、无 Bearer | HTTP 401 |
| 同一查询，只携带 OAuth2 Bearer | HTTP 200，业务 code=0，返回 2 个模型类型 |
| 功能仓详情，只携带 Bearer | HTTP 200，业务 code=0，包含 permissionCodes 等字段 |
| 功能仓版本分页，只携带 Bearer | HTTP 200，业务 code=0 |
| dev Portal 获取的 OAuth2 Bearer，跨域查询正式功能仓模型类型 | HTTP 200，业务 code=0 |

身份为 guofeng.su，issuer 为 `https://auth.wellspiking.ai/auth/realms/westwell`。解码用于查看的 exp 显示测试时正式会话令牌剩余约 20 小时，dev 会话令牌约 21.7 小时；这不是固定有效期配置的证明。线上平台 middleware 本身会验证 OIDC 令牌签名、issuer 和 audience，新增转发必须在认证与资源授权之后执行，不能直接信任任意来访者填写的头。

验证边界：这是实际浏览器取得 oauth2-proxy 令牌后调用功能仓的只读认证实验，不是已部署的 RayTrain 后端连接器；未验证上传、创建版本或目标写权限。已证明不必把单独服务账号作为用户即时操作的必要前提。

异步自动同步仍有不同要求：训练及重试可能超过 access token 有效期；该请求头不提供 refresh token，也不能让后台通过已失效的 access token 自行续期。实施前需确认受支持的用户委托/续期机制，或使用目标受限的服务身份执行已授权任务。若首批只支持有效期内转发，过期时必须显示待重新授权；不能把这一限制称为完整的无人值守自动同步。

用户令牌只能发往管理员配置且经过校验的功能仓地址，不允许用户任填 URL，上传重定向不能携带令牌跳往任意地址；不得将令牌放进任务规格、普通同步记录、训练容器或日志。

## 12. 首个实现候选与真实接口差异

本地候选新增功能仓查询连接器与平台内部发现路由，尚未发布：

- `GET /api/v1/function-warehouse/environments`
- `GET /api/v1/function-warehouse/:environment/warehouses`
- `GET /api/v1/function-warehouse/:environment/warehouses/:warehouseId/model-types`

固定 `production/development` 的域名和用户提供的 groupId。只有经过 OIDC 验证并映射到有效平台成员的本次请求令牌可以转发；本地登录、PAT 或仅填写同名代理头不能获得委托权限。发现路由仅输出名称、ID、权限码等选择信息，不输出上游文件路径、令牌或任意元数据。

2026-09-14 本轮实际读取 dev `/model/v2/api-docs` 返回 HTTP 200：`创建模型版本信息` 仅包含 description、functionWarehouseId、groupId、modelTypeId、paths、production、recommended、version。该观察只能证明当前接口文档尚未声明训练来源，不能据此证明后端忽略字段；仍需专用创建与读回实验确认。后续只读探测遇到自动审批服务容量错误，未完成实际写入实验。

这个候选不包含上传任务、训练完成自动触发、持久化同步记录或 Portal 操作按钮；不能作为自动同步已交付的证据。首个红灯测试已在构建机确认缺少已验证 OAuth 委托接口时编译失败，随后补上令牌传递实现。完整候选的提交和 bundle 操作被自动审批服务容量错误阻止，绿色测试尚未执行；静态安全审阅未发现阻塞问题，不能替代构建机测试。

## 13. 后续实现与联调更新（尚未部署）

继续开发后，查询基础候选 `73809fd` 已在构建机隔离 worktree 通过 `go test ./auth ./functionwarehouse ./api`。正在整合 schema53 持久同步记录、加密用户委托、租约/取消/重新授权、20 GiB 上限和大文件分片、严格唯一 Run 来源解析及 Portal 任务详情入口；这些新增部分需要单独完成完整候选验证，不能沿用基础查询的测试结论。

功能仓 dev Swagger 已增加**顶层** `jobId`、`runId`、`experimentId`，实际对接以此为准，不使用第4节待讨论的嵌套 trainingSource 示例。已有评估 `mlflowRunId/mlflowExperimentId` 保留。用户已补充 guofeng.su 的开发团队成员权限。

本轮专用测试资源：功能仓 `35b9cdf6ebd2d8afd95093174ea16d5b`、模型类型 `a55649782265452e6396af87641819db`、版本 `98532580dd0ef51beac959b3b8cfa34f`。仅上传100字节合成文件，非业务权重；SHA-256 为 `2e011ee9444b22c18b4b17b6385c68e43fa34fdfb827349f3134763a4c1495a4`。创建版本使用明确标注协议验收的合成来源 ID，不代表真实训练闭环验收。

上传接口返回 HTTP200/code0，文件名、大小和摘要一致；文件URL为 `/system/api/file/preview/...` 站内相对地址，客户端已据此补兼容测试。早期版本创建返回三个来源 ID，但分页字段为 null；功能仓后端已修复，现已读回一致。平台仍在创建后再次核对来源，不一致时为 UNKNOWN/待确认，不报同步成功，不自动再次创建。

首版入口在「本人任务详情 → 训练产物 → 功能仓同步」：运行中可配置训练成功后自动同步，结束后可立即同步；不修改既有训练提交规格、CLI或训练容器。提交页面自动携带同步配置属于后续便利性改进，不应把该入口描述为已经存在。用户在页面选择目标仓和文件，用户委托失效后需重新登录并重试；没有刷新令牌就不承诺完全无人值守。

后台实现及通用 `code=-1` 不确定创建结果已修复并提交，Portal 候选基于 dev `ef5c18a037ef148fe62a82216a8ac3157998e641`，工作区 `/tmp/rtp-portal-warehouse-20260914`。此前自动审批服务容量错误已不再阻塞候选验证；完整 Go/PostgreSQL/Portal 结果见第14节。仍以真实部署和平台闭环验收为交付标准。

合成文件下载已实际返回 HTTP200、100字节，SHA-256 与上传源一致。功能仓开发测试资产继续保留用于修复来源字段后的联调，不处理其他业务资产。后续先完成完整候选验证及功能仓来源读回，再准备 schema52→53 的具体备份/恢复验证并取得本次授权，最后按发布闸门上线；没有复用以前的生产数据库备份授权。


## 14. 来源修复与发布候选验证（2026-09-14，尚未部署）

第12、13节记录的是当时的阶段结果，以下为更新后的证据：

- 功能仓后端已修复来源字段的查询读回。再次查询版本 `98532580dd0ef51beac959b3b8cfa34f`，jobId/runId/experimentId 均与创建请求一致。
- 同一组合成来源选择第二个101字节文件，成功创建独立版本 `729b1ae1cd0af8452fd01350d6fc8882`，并再次分页读回三个来源及文件摘要。第二文件 SHA-256 为 `465c53bc7b443a60dd50be2f7c318a6d9f3641ed82570aa69b7234e0d2127b5f`。这验证主动重复创建的上游合同，不代表平台自动任务闭环已验收。
- 后端候选 `337184f` 在构建机通过全部 `go test ./...`；functionwarehouse 与 warehousesync 的 race 检测通过，覆盖率分别84.2%和80.0%。格式化在构建机执行，返回本地审阅并提交。
- 无网络隔离 PostgreSQL 验证通过：schema52→53升级、重复迁移、旧身份/模型数据保留、旧后端写入形状兼容，以及并发配额、幂等键、领取租约、CAS、取消与登记互斥。临时测试容器已删除。
- Portal 候选 `990045c4` 基于 dev `ef5c18a0`，完整 Dockerfile.lint 门禁、6个新增功能仓合同测试及 Vite development 构建通过。未在本机安装依赖、测试、格式化或构建；归档传输前清空已跟踪环境文件的凭据类值。
- 当前正式构建目录及两个后端远端仍为 `2f565a0c`，Helm230，后端2/2健康，运行摘要 `sha256:52288f1862275e84ca168b7666b94a3680f95b8591b51f47a185588418636956`。发布前仍需重新核对。

待完成：生产schema52备份与隔离恢复授权/执行、最终审阅、四端与Portal同步、最小后端部署、登录浏览器与平台后台实际同步验收。严格区分上游协议验证与已部署的训练平台闭环。尚未验证真实大文件分片吞吐或长期令牌续期；当前仍以待重新授权状态处理过期委托。

真实开发环境的分片协议验收：3字节合成内容分为两片，chunk/upload、chunk/merge、upload/process 均HTTP200/code0，状态done；下载HTTP200，3字节与SHA256一致。此结果验证协议，未验证500MiB以上吞吐。平台闭环计划使用本人已结束验收任务 `job-078243ab019985c842f9f74b` 中既有156字节合成权重，无需新训练或读取业务权重。

## 15. 实际发布与用户入口收敛

以上“尚未部署”均为历史阶段。本节及第16节为本轮最终验证记录。

- 已按本次授权备份生产 schema52，在无网络 PostgreSQL16 容器中恢复验证，删除恢复容器；受限备份保留在构建机 `/root/raytrain-release-20260914-function-warehouse`，目录700、数据库文件600。schema53 为新增同步记录表，不改写原训练数据。
- 后端功能首发 Helm231，联调发现功能仓会异步调整 `filePath`；`d0dd884` 修复仅排除该可变路径的等值比较，继续严格核对 URL、文件名、大小、SHA-256、范围和三个来源 ID。构建机 RED→GREEN、全量 Go 及 race 通过；functionwarehouse/warehousesync 覆盖率84.5%/80.0%，Helm232 发布。
- 用户入口按最新要求固定正式功能仓，不再选择目标环境。Portal 候选 `77d266b7a75a7f01c85b154ff6b4107866ce8fc2` 保留同事的最新 dev 更新，构建机完整 Dockerfile.lint、7个功能仓合同测试及 Vite 构建通过；GitLab [pipeline33944](https://gitlab.wellspiking.ai/wellspiking/frontend/wellspiking-frontend/-/pipelines/33944) 的 lint89974、docker89975、helm-deploy89976 全部成功。登录浏览器验证11个原菜单保留、表单无环境选项、固定正式仓、按钮实际创建成功。
- 平台使用说明已更新既有模型登记文章，未新增一套重复文档：任务详情→训练产物→同步到功能仓，运行中“保存自动同步”，结束后“开始同步”；不要求先去实验中心登记模型。登录帮助页面确认新文案存在，旧“选择开发或正式环境”消失。
- 当前后端运行版本 `release-20260914-05-74763d9`，Helm **233**，schema **53**；摘要 `sha256:708bc8cfdd72b42c3a94d2aca10d5e58415326b2b48ed60c22d7cd2d98045033`。两个副本 Ready、0重启。三次最小发布 dry-run 仅改 backend 镜像，前后既有训练 RayJob/RayCluster/Pod UID、状态和重启数差异为空。
- 后续 `e07aa852e43f977a40e47fb505ef99bdea87282a` 修复通用 MLflow 示例：普通 ray-ddp 不注入 `RAYTRAIN_CLUSTER_ATTEMPT`，示例应省略该可选 tag，仍严格要求 Job/租户/用户/provenance。构建机3项测试及参数分支 RED→GREEN 通过；代码同步四端，但不进入训练镜像，无需重建后端或训练镜像。

开发仓历史手动验收记录：`3f7de0e4-6adb-48d1-bae7-d84c3bb00f51` 成功，目标版本 `f6f5778342859560829d02b8bc5bac65`，156字节文件下载与摘要一致。首次路径联调的 UNKNOWN `b825406e-4de3-4120-bdf9-d0a26b772126` 保留原状态，不重复创建、不自动改写成功；相同请求键复用原记录，UNKNOWN 重试返回409。两份平台验收副本已归档。

## 16. 正式功能仓真实训练、浏览器和自动触发验收

用户单独授权新建正式私有验收仓 **RayTrain训练同步验收20260914**：仓 ID `03478e6e89a13b2b2ed8cc55aaf894c5`，模型类型 `4ebd7b2f68ecf67515cb6dbcb0e97112`。所有正式写入仅此仓，不改既有业务仓，不创建发布仓版本。上游版本字段 `production=false` 表示未设为生产发布版本，**不表示同步到了开发环境**；目标域名为 `spiking.wellspiking.ai`。

用户指定历史任务 `job-286611e35c509c4c185ea04c` 属于 guofeng.su，SUCCEEDED，包含441088807字节 `epoch_1.pth`。查询当前8个 MLflow 实验，以任务标签、任务 ID Run 名称和训练名称检索，均未发现其 Run；平台任务 experiment 接口也无 Run。未上传这份业务权重，未补造历史 Run，不能声称它已关联三个 ID。

用户逐次授权专用1Worker/1GPU/4CPU/16Gi随机张量训练：

| 验收 | 真实结果 |
| --- | --- |
| 首次 `job-80ff103227e139a4b068c7c5` | 示例错误要求可选 cluster_attempt，训练失败；同步 `6ba9191f-9c33-4ad3-b795-15c452ccc832` 随后 FAILED，无文件上传。已修复示例，未把失败算通过。 |
| 修复后 `job-e3393adf64d4321c0d4bc7a1` | 30步 GPU 训练成功，Run `ca3b20ae4da444cf96705e13d3224d58` / Experiment `1`。同步 `294b517d-9473-433f-92b9-2728d74d20c1` 成功，目标 `0a54855bbef3362130dbe443b5c8526c`。配置比训练结束晚约3秒，仅证明自动模式处理已结束任务，不冒充结束触发验收。 |
| 新版浏览器按钮 | 同一已结束任务真实操作“新建同步→正式仓→目标模型→开始同步”；记录 `34dec81f-d673-4f47-8ebd-fd5af24ebbbc` 成功，目标版本 `14c4557852084e1c0a3263e421f6651c`。页面三个 ID、SHA-256、正式仓链接均正确，同任务主动创建第二版本符合用户要求。 |
| 完整自动触发 `job-8cfb0f036d230537d1806b28` | 最新 CLI `release-20260911-11` 提交；**RUNNING 时**通过已登录同域接口保存配置，记录 `0d2cc63a-8b59-4783-b409-9b4866d80713` 于09:52:38Z创建，09:55:43Z仍为WAITING_SOURCE。任务09:58:17Z成功，同一记录09:59:19Z自动SUCCEEDED，期间未再次创建或重试。Run `e83b7a61be0242cdb7f95840634cca87` / Experiment `1`；正式目标版本 `2f4868260a582d201d202677a4ebc3a3`。 |

两份成功训练文件均为1548字节 `model.pt`，不是业务模型。首份 SHA-256 `a30d8b08bb4bc6a0efc8073d98271eabeb3305de1b2387d7cc59ef086e03d516`；完整触发 SHA-256 `ef2737f9934138beb0df058450d1d4bfa1b255715b96a557748952f2da8e70be`。正式仓版本分页读回三个 ID 一致，从正式文件接口下载HTTP200，实际字节数及重新计算 SHA-256 均匹配。

收尾：三枚临时 PAT `pat-5086f8d405e09d9daa5c30e9`、`pat-968832ac7616470cd7a5a10a`、`pat-73289b1890259ee54fdc3897` 均已撤销并读回 revokedAt；构建机专用登录配置已删除。三个正式平台模型副本已归档（`b943d6a7-33c1-4aa0-a7ec-70841297343e`、`8c948f25-5886-4a5c-934b-4a6417898fd3`、`fbf5cbc9-be93-4718-82d2-b47467d79b2b`），保留功能仓版本和审计。数据库只读验证本轮四条正式操作的 credential 长度均0、lease_id为空。配额保持24，验收前后均已用8/可用16。未变更用户正在运行的训练、调度或个人数据。

边界：只有本人当前团队任务可发起同步，目标功能仓权限继续由功能仓检查；模型可见性和发布规则未扩大。没有或无法唯一验证的 Run 会阻止完整来源同步。大文件分片协议已验证，但500MiB以上实际吞吐未验收；长期委托仍可能要求重新登录。功能仓后端已保存三个结构化 ID，训练平台同步记录展示三者；最后登录正式功能仓并打开“模型查看”，确认文件显示 DEFAULT（成功）并有下载按钮，但该对话框尚未显示三个来源 ID，后续由功能仓页面补充展示，不等于发布仓流程完成。
