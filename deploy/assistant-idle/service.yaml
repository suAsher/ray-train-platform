apiVersion: v1
kind: Service
metadata:
  name: assistant-idle-controller
  namespace: PLACEHOLDER_ASSISTANT_NAMESPACE
  labels:
    app.kubernetes.io/name: assistant-idle-controller
    app.kubernetes.io/instance: PLACEHOLDER_INSTANCE_ID
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: assistant-idle-controller
    app.kubernetes.io/instance: PLACEHOLDER_INSTANCE_ID
  ports:
    - name: http
      port: 8080
      targetPort: http
