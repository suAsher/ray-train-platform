package httpapi

// SourceArtifactIDHeader binds a successful Ray-compatible package upload to
// the owner-scoped source artifact that the platform persisted for it.
const SourceArtifactIDHeader = "X-Ray-Platform-Source-Artifact-ID"
