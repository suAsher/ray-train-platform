package helpdocs

const environmentImageGuide = `### 先判断改的是代码还是环境

只修改 train.py、配置或模型结构时，重新上传源码即可；训练镜像用于提供 Python 和依赖。新增依赖后，可以从「交互式调试」保存一个环境版本。原有 Base 训练镜像仍可继续使用，新增调试镜像不会替换原镜像或默认选项。

### 调通后如何保存训练环境

1. 在「交互式调试」选择支持保存环境的 Base 调试镜像，创建自己的工作区。已有其他类型的工作区不会自动转换。
2. 打开 VS Code 终端或对应的 Jupyter 内核，确认 Python 路径为 /opt/raytrain/environment/bin/python。在这个受管环境中安装并测试需要的依赖，使用 python -m pip，避免调用其他环境的 pip。新增 Base 调试镜像已配置火山内网源 https://mirrors.ivolces.com/pypi/simple/，例如 python -m pip install requests；原有环境不自动更改配置。
3. 在工作区仍运行时点击「保存当前调试环境」。填写名称，选择仅本人或当前团队可用。此范围控制平台镜像目录与训练提交权限；Harbor 仓库自身的可见范围由 Harbor 项目权限决定。
4. 填写自己的 Harbor 用户名和 CLI Secret，选择有写权限的项目及仓库名。公司 Harbor 使用单点登录，CLI Secret 在 Harbor 个人资料中取得；不是平台 PAT，也不是公司登录密码。不要把凭据写入代码、终端命令或截图。
5. 点击「验证目标写权限」，通过后点击「构建并推送」。推送地址为 harbor.wellspiking.ai/<所选项目>/<镜像名称>:<自动版本>；项目和镜像名称由你填写，没有固定到某个人的项目。排队和捕获期间请勿继续安装或修改依赖，平台以实际捕获时的依赖为准。平台使用集群 CPU 与临时磁盘重建环境、校验文件，再用你的凭据推送到所选 Harbor 仓库。标签由平台生成，每次保存产生一个新版本。
6. 等待状态变为“可用于训练”（READY），点击「用于训练」。页面带入固定摘要镜像，继续选择训练源码、数据和资源后提交。READY 表示镜像构建与拉取检查通过，仍需用自己的小规模训练验证业务代码与 GPU 兼容性。

### 保存后，浏览器怎样提交训练

环境版本只保存依赖，不包含训练代码。点击「用于训练」带入镜像后，还要选择一个代码版本：

- 代码在本机：在「数据与存储 → 我的工作区」上传源码 ZIP 并创建任务；ZIP 根目录放启动脚本，随后在任务表单选择刚保存的环境。
- 代码已在工作区：进入自己的项目子目录，点击「创建训练代码版本」，用生成的快照创建任务。先确认当前目录；开发中心的快捷快照覆盖整个工作区根目录，不适合混放多个项目的工作区。
- 代码在 Git：在创建任务页输入平台能访问的仓库地址及分支/标签，点击解析并确认固定 commit。修改地址或分支后需要重新解析。训练节点不需要直接访问 GitHub；平台准备源码包后从内网提供给任务。

接着填写相对于源码根目录的启动命令，选择数据位置和资源。先用 1 Worker、1 GPU 跑小样本。数据目录通过 PLATFORM_DATASET_PATH 读取；checkpoint、报告等保存到 PLATFORM_OUTPUT_PATH。不要把调试工作区的绝对路径直接当作训练源码路径。

任务成功后，在任务详情查看日志与训练产物；在环境版本上保存成功不代表业务代码已完成训练验收。

### 同一环境怎样通过命令行提交

在「账户与安全」创建含 jobs:read、jobs:write、sources:write 的个人访问令牌，在自己的电脑安装平台 CLI。训练令牌与推送镜像用的 Harbor CLI Secret 是两种凭据。

~~~bash
# 登录会交互提示输入平台 PAT；不要把令牌直接写进命令历史。
spk-rayjob login --server https://raytrain.wellspiking.ai --token-stdin
spk-rayjob images

# 进入包含 train.py 的本地源码目录，替换镜像摘要和启动命令。
spk-rayjob submit \
  --dir . \
  --name my-training \
  --image 'harbor.wellspiking.ai/<项目>/<镜像>@sha256:<保存版本的摘要>' \
  --engine ray-ddp \
  --execution-mode single_gpu \
  --entrypoint 'python train.py' \
  --workers 1 --gpus-per-worker 1 \
  --cpu-per-worker 4 --memory-per-worker 16Gi \
  --output-path experiments/my-training \
  --watch
~~~

上面的命令适用于不读取外部数据的最小训练。如果需要个人数据，加 --input-space my-files --input-path '<个人数据相对路径>'；不要填写 /mnt/storage/me/... 这样的容器绝对路径。数据版本、托管 Ray Train、多卡及续训请使用对应使用说明，不能只把单卡示例的 Worker 数量调大。

--dir 会上传本地源码，不会自动取得网页选中的 Git commit 或工作区快照。要比较两种入口，请先准备相同版本的源码。保存环境为「仅本人」时只有本人能在当前团队选择并提交；「当前团队」允许当前团队成员使用，浏览器、CLI 和原生 Ray 入口执行相同的镜像准入检查。

### Ray Dashboard 什么时候能打开

在自己的任务详情点击「Ray Dashboard」。需要任务正在运行且 Ray Head 已就绪；排队或初始化阶段请稍后重试。入口会建立短期的任务访问会话，不要复制跳转中的一次性票据给他人。任务结束并回收集群后，Dashboard 不再可用，历史日志、产物和实验指标继续在平台查看。

### 哪些内容会保存

当前自动保存支持受管 Python 环境中可重建的 wheel 依赖。平台核对安装文件、固定依赖版本和材料摘要；不会把整个容器、工作区代码、数据集、权重或个人目录复制进镜像。训练代码继续作为独立源码提交。

原 Base 的 Ray、PyTorch、CUDA 等核心依赖不能通过此入口替换；系统软件、Conda 环境、可编辑安装、未提供可重建材料的本地包或手动修改依赖文件，会明确拒绝保存。此类环境需要单独准备并登记专用镜像。

停止或重建调试工作区可能丢失尚未保存的依赖，先等待环境版本 READY 再停止。工作区文件快照只保存其声明的文件范围，不能代替环境版本。

### 构建失败、取消或凭据过期怎么办

- 查看环境版本详情中的失败阶段和提示。凭据失效或项目无写权限时，重新授权后重试同一条记录；不要把“能看到项目”当作“能推送仓库”。
- 找不到所需 wheel 或文件校验失败时，按提示补齐可用依赖材料或修正安装方式，再创建新版本。训练节点不需要从 GitHub 获取训练源码。
- 取消仅停止本次构建，不停止工作区或训练任务。已推送到 Harbor 的镜像不会因取消而被自动删除。
- 平台授权最长保留 24 小时，操作结束后清理本次推送凭据。清理平台副本不等于撤销 Harbor 本身的 CLI Secret；如需撤销，前往 Harbor 处理。

环境构建使用独立的集群 CPU 和临时磁盘；训练与 GPU 调试仍按当前团队配额提交。构建镜像不会自动启动训练，也不会更改已有任务使用的镜像。`
