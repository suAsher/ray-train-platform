package helpdocs

const modelEvaluationStartID = "model-evaluation-start"
const modelEvaluationResultsID = "model-evaluation-results"

const modelEvaluationStartGuide = `独立评估用已经保存的权重，在明确的数据划分上计算指标。它会创建新的计算任务，保留独立记录，不修改原训练任务或权重。

### 从模型版本开始

1. 打开「实验中心 → 模型」，选择未归档模型的 READY 版本，点击“发起评估”。模型版本和权重 SHA-256 固定，不能在同一次评估中替换。
2. 选择“评估方案”。方案说明使用哪份评估代码、运行环境和数据格式；代码提交和镜像摘要不可变。没有适用方案时，需要先准备能够加载该模型的代码和环境，由平台管理员登记。仅有 .pth 文件不够，模型结构、预处理、类别映射和指标计算必须与权重匹配。
3. 选择有权使用的具体 READY 数据版本，明确选择验证集 val 或测试集 test。评估不接受 latest，也不会在版本失效或划分为空时自动换数据；样本数为 0 的划分不能提交。当前流程使用所选划分的全部场地。
4. 填写方案所需参数及 Worker、GPU、CPU、内存等资源，运行预检，核对权重 SHA、数据版本摘要、划分和样本数量、代码提交、镜像及资源。确认后再提交。
5. 在「实验中心 → 评估」查看记录与状态。评估和训练使用同一套资源配额与排队机制；资源不足时需等待或减少资源，不能借评估绕过配额。

参数是评估代码读取的配置。不要填写口令或令牌；平台已有任务专属凭据负责下载这次选定的权重和写回报告，代码无需个人 PAT。评估代码必须遵守页面所示报告协议，在任务中实际读取指定权重和数据并计算指标；平台不会从普通日志推断报告。

### 数据集、报告与共享范围

模型版本中的训练数据来源回答“训练时用了什么”；本次评估数据回答“现在用什么衡量它”。两者可以不同，平台分别保存，不会互相覆盖。训练数据未知时仍保持未知。

PUBLIC 数据集的评估记录和报告可供登录成员查看；TEAM 数据集的结果保留同团队或平台管理员可见的边界。模型权重共享不会公开团队原始数据。其他成员能看报告，不代表能打开执行者的私人任务日志或结果目录。

固定评估来源的目的是可复现，不代表模型效果已经合格。结果和比较见[评估报告在哪里看，为什么不能比较？](#model-evaluation-results)。`

const modelEvaluationResultsGuide = `在「实验中心 → 评估」选择一条记录，查看执行状态、报告状态、固定来源和指标；也可以下载 JSON 报告留存。报告中的指标由该方案的代码计算，平台只校验格式、来源与任务关系，不会替业务判断达标阈值。

### 执行状态和报告状态为什么分开

| 现象 | 含义与处理 |
| --- | --- |
| 等待创建、已提交或运行中 | 等待任务创建、资源准入或计算结束；有权访问任务时可查看日志 |
| 任务成功退出且报告有效 | 本次评估完成，可以查看指标与来源 |
| 任务成功退出但报告缺失 | 评估未完成；检查方案是否在退出前成功上报，不能只打印指标到日志 |
| 报告无效或上报被拒绝 | 检查协议、评估 ID、权重/数据/配置摘要、数值和指标名称；必须使用本次任务的来源 |
| 任务失败、超时或取消 | 查看任务原因；已有部分输出也不当作一次成功评估 |

只有发起者或平台管理员可以取消尚未结束的评估。修正代码需要新方案，修正参数或重跑需要新评估记录；历史报告不可覆盖。先保留错误和来源信息，再重新发起。

### 如何比较两个结果

选择两条成功且报告有效的评估。平台要求数据 manifest、划分、场地、评估方案和配置一致；比较不同模型版本时仍需保持这些条件相同。指标名称、单位或方向不一致也不能直接比较。

例如同一个模型在 val 与 test 上的数值，或两个模型使用不同数据版本得到的指标，不能直接当作同口径优劣结论。页面会说明不可比较的原因，需要重新用相同条件运行。

数值零是有效结果，不显示成缺失。指标方向为“越大越好”“越小越好”时，差值按对应方向解释；中性指标只比较数值，不自动评优。分组指标应结合分组样本量阅读，不能只看总平均。

### 与 MLflow、审批和推理有什么关系

独立评估报告和 MLflow 的 Run 是不同记录。评估代码可以通过已有 MLflow 接入另记实验指标，但写入 MLflow 不会替代本流程的报告，也不会自动通过审批。审批和推理服务尚未上线，评估完成不代表模型已获准发布或已经提供推理接口。`
